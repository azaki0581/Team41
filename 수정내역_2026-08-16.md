# 냉장고 털기 — 수정 내역 (2026-08-16)

> 이 문서는 `index.html` 하나에 들어있는 추천 로직/화면을 코드를 직접 열어보지 않아도
> 무엇이, 왜, 어떻게 바뀌었는지 알 수 있도록 정리한 문서입니다. 하루 동안 진행한
> 모든 수정 사항을 시간순으로 한 파일에 모았습니다 (이전에 나눠뒀던 v1/v2 문서를
> 이 파일 하나로 재통합했습니다).
> 실제 코드는 전부 [`index.html`](./index.html) 안의 `<script>` 태그 안에 있습니다.

## 목차

기획서 "9. 다음 단계"에 적혀 있던 계획 — **2차**: 재료 입력 자동완성, 재료 별칭
사전 보강, 대체 재료 제안 / **3차**: 리뷰·평점, 장보기 리스트 자동 생성, 2인분
분량 변환 — 을 기준으로, 그 전에 먼저 처리한 정렬 개선·필터까지 포함해 총
9개 항목을 반영했습니다.

1. 추천 정렬 순서 개선
2. 요리 종류 필터
3. 재료 입력 자동완성 *(2차)*
4. 재료 별칭 사전 보강 + 원본 데이터 오류 수정 *(2차)*
5. 대체 재료 제안 *(2차)*
6. 장보기 리스트 자동 생성 + 쿠팡 연결 *(3차)*
7. 2인분 분량 변환 *(3차)*
8. 리뷰/평점 → "내 평가"로 범위를 좁혀 구현 *(3차)*
9. 재료 키(key) 정리 — "양파다진것"/"양파(½개)"/"양념들기름" 같은 원본 데이터 노이즈 제거
   (총 4라운드에 걸쳐 진행: 1차 정리 → 5개 이하 재료 전수 조사·대량 별칭 보강
   → "재료ooo"/">" 패턴 추가 정리 → rules.json↔index.html 동기화 격차 수정.
   최종적으로 고유 키 1,660개 → **992개**까지 줄였습니다.)

---

## 1. 추천 정렬 순서 개선

### 무엇이 문제였나
같은 재료를 넣었을 때, **당장 살 것 없이 바로 만들 수 있는 요리**보다
**재료를 3개나 더 사야 하는 요리**가 위에 뜨는 경우가 있었습니다.

예시 (재료: 대파·밥·김치·두부·감자·당근·버섯·돼지고기·계란·양파 + 기본 양념):

| 순위(수정 전) | 요리 | 상태 |
|---|---|---|
| 1위 | 삼겹가지볶음 | 가지·깻잎·잣 **3개 부족**, 게다가 고기 재우는 데 **2시간** 걸림 |
| 2위 | 불고기 소스 두부구이 | **바로 만들 수 있음** |
| 3위 | 새송이등갈비 | **바로 만들 수 있음** |

### 왜 이런 일이 생겼나
예전 로직은 "내가 가진 재료를 몇 개 쓰는가"와 "몇 개를 더 사야 하는가"를
하나의 점수로 뭉쳐서 계산했습니다. 삼겹가지볶음처럼 가진 재료를 16개나 쓰는
요리는, 3개가 부족해서 감점을 받아도 점수가 높게 나와 버렸습니다.
("많이 쓴다"는 점수가 "덜 준비됐다"는 감점을 이겨버림)

### 어떻게 고쳤나
점수를 하나로 뭉치지 않고, **우선순위를 단계별로 분리**했습니다.
같은 단계에서 동점이면 다음 단계로 비교합니다.

1. **바로 만들 수 있는 정도** — 부족한 재료 수가 적은 요리가 최우선
2. **인기 메뉴** — 사람들이 많이 선택한 요리 (☆아래 참고)
3. **재료를 많이 쓰는 요리** — 냉장고를 많이 비울수록 우선
4. **조리 시간이 짧은 요리** — 재우기·숙성 등 대기시간까지 합쳐서 비교
   (예: 삼겹가지볶음은 "9~14분"이라고 적혀 있어도 재우는 데 2시간이 걸리므로,
   이 대기시간까지 더해서 비교하도록 했습니다)

☆ **인기 메뉴(2번) 관련 안내**: 이 앱은 서버 없이 브라우저에서만 동작하는
정적 페이지라서, 지금은 "다른 사람들이 얼마나 많이 선택했는지"를 모을 방법이
없습니다. 그래서 지금은 이 기준이 항상 무승부로 처리되어 건너뛰어집니다.
나중에 실제 서비스로 붙여서 선택 데이터를 모으게 되면, `recipes.js`의 각
레시피에 `popularity` 값(선택된 횟수 등)만 채워 넣으면 이 로직이 자동으로
살아나도록 미리 구조를 짜 두었습니다. (즉, 코드 추가 수정 없이 데이터만
채우면 됩니다.)

### 결과 확인
위 예시 재료로 다시 테스트한 결과:

| 순위(수정 후) | 요리 | 상태 |
|---|---|---|
| 1위 | 불고기 소스 두부구이 | 바로 만들 수 있음 |
| 2위 | 새송이등갈비 | 바로 만들 수 있음 |
| 3위 | 고구마순나물 | 1개만 더 있으면 됨 |

삼겹가지볶음은 더 이상 상단에 나오지 않습니다.

**코드 위치**: `search()` 함수

---

## 2. "요리 종류" 필터 추가

입력 화면의 **양념·조미료 칸 아래, 추천받기 버튼 위**에 요리 종류를
고를 수 있는 필터를 추가했습니다.

- 선택 가능한 항목: **전체 · 밥 · 반찬 · 국&찌개 · 후식 · 일품 · 기타**
  (레시피 데이터 1,151건에 실제로 존재하는 카테고리를 전부 넣었습니다 —
  밥 119건 · 반찬 570건 · 국&찌개 102건 · 후식 142건 · 일품 181건 · 기타 37건)
- 여러 개를 동시에 선택할 수 있습니다 (예: "밥"과 "반찬"을 함께 선택 가능)
- 아무것도 고르지 않으면 "전체"가 자동으로 켜진 상태가 되어, 필터 없이
  전체 레시피에서 찾습니다. 하나라도 직접 고르면 "전체"는 자동으로 꺼집니다.
- 필터링 결과 요리가 하나도 없을 때는, "고른 요리 종류가 너무 좁은 건
  아닌지 확인해보라"는 안내 문구가 함께 뜹니다.

### 결과 확인
같은 재료로 "반찬"만 선택 → 결과가 전부 반찬으로 나옴. "반찬"을 끄고 "밥"만
선택 → 찬밥이용닭죽 / 밥크로켓 / 마늘 불고기 덮밥 등 밥 카테고리 요리로 결과가
바뀌는 것을 확인했습니다.

**코드 위치**: `index.html` 내 "요리 종류 필터" 섹션, `search()` 함수 안의 카테고리 필터링 로직

---

## 3. 재료 입력 자동완성 (2차)

입력창에 글자를 치면, 레시피 1,151건에 실제로 등장하는 재료 **1,660종** 중
지금 입력 중인 글자로 시작하거나 포함하는 것들을 최대 8개까지, 자주 쓰이는
레시피 수가 많은 순으로 보여줍니다.

- 방향키(↑↓)로 이동, Enter로 선택, Esc로 닫기 — 키보드만으로도 조작 가능
- 마우스 클릭으로도 선택 가능, 선택하면 바로 재료 칩으로 추가됨
- 이미 담은 재료는 추천 목록에서 자동으로 빠짐

**참고로 확인해둘 점**: 원본 데이터의 "재료 키(key)" 1,660개는 완전히 정리된
동의어 목록이 아니라, 원본 API가 준 재료 이름들을 일부만 정규화한 것이라서
"돼지고기", "돼지등심", "돼지고기(삼겹살)" 처럼 사람이 보기엔 같은 재료인데
서로 다른 키로 남아있는 경우가 꽤 있습니다. 자동완성은 이 데이터를 있는 그대로
보여주므로, 겹쳐 보이는 항목이 나오는 건 자동완성 기능의 버그가 아니라
원본 데이터의 특성입니다. (4번 별칭 보강이 이런 문제를 부분적으로 줄여줍니다.)

**코드 위치**: `index.html` 내 "재료 입력 자동완성" 섹션 (`$suggest`, `renderSuggest()` 등)

---

## 4. 재료 별칭 사전 보강 + 원본 데이터 오류 1건 수정 (2차)

사용자가 입력창에 자주 칠 법한 표현 30여 개를 추가로 별칭 처리했습니다.
(예: "국간장/진간장/양조간장" → "간장", "다진마늘" 계열, "생새우/새우살" → "새우" 등)

**오류 수정 1건**: 원본 별칭 데이터에 `"참치": "참치캔"` 이라는 항목이 있었는데,
정작 레시피들은 재료 키를 `"참치캔"`이 아니라 `"참치"`로 쓰고 있었습니다. 즉
사용자가 "참치"라고 입력하면 오히려 존재하지 않는 키로 바뀌어버려서, 참치가
들어간 레시피를 하나도 찾지 못하는 상태였습니다. 이번에 바로잡았습니다.

이 보강 내용은 두 군데에 반영했습니다.
- `index.html` 의 `EXTRA_ALIASES` — 지금 당장 브라우저에서 바로 적용됩니다.
- `tools/rules.json` 의 `userInputAliases` — `recipes.js`를 다시 만들 때도
  유지되도록 원본 규칙 파일에도 똑같이 반영해뒀습니다. (`recipes.js`는 자동
  생성물이라 직접 고치지 않는다는 원칙을 지켰습니다.)

**코드 위치**: `index.html` 의 `EXTRA_ALIASES`, `tools/rules.json` 의 `userInputAliases`

---

## 5. 대체 재료 제안 (2차)

레시피에 부족한 재료가 있을 때, 사용자가 **이미 갖고 있는 다른 재료로 바꿔
쓸 수 있으면** 알려줍니다. 예: 돼지고기가 없고 닭고기가 있는 경우 →
"🔄 돼지고기 대신 **닭고기**로 대체해볼 수 있어요."

갖고 있지도 않은 재료를 대안으로 들이미는 건 의미가 없으므로, 실제로 소유한
재료만 골라서 보여주도록 했습니다. 돼지고기↔닭고기↔소고기, 버터↔식용유,
설탕↔올리고당↔꿀 등 흔히 쓰이는 조합 위주로 20여 개 매핑을 담았습니다.

### 결과 확인 (직접 재현하는 법)
1. 재료 입력창에 **`대파, 당근, 양파, 버섯, 닭고기`** 5개만 넣기 (돼지고기는 빼고)
2. 양념·조미료는 기본값 그대로 두고 추천받기
3. 결과의 **"새송이등갈비"** 카드에서
   `🛒 돼지고기만 있으면 완성돼요.` 아래에
   `🔄 돼지고기 대신 닭고기(으)로 대체해볼 수 있어요.` 가 뜨는 것을 확인

원래 레시피가 요구하는 재료(돼지고기)는 빼고, 그 대체재(닭고기)를 대신 넣어야
문구가 나타납니다 — 이미 둘 다 갖고 있으면 애초에 "대체할 필요"가 없으므로
문구가 뜨지 않습니다.

**코드 위치**: `index.html` 내 `SUBSTITUTES` 테이블, `subNoteHtml()`

---

## 6. 장보기 리스트 자동 생성 + 쿠팡 연결 (3차)

레시피 카드마다 "🛒 부족한 재료 장보기 리스트에 담기" 버튼이 생겼습니다.
누르면 그 레시피에 부족한 재료들이 화면 우측 하단에 떠 있는 장보기 리스트에
쌓입니다. 여러 레시피를 훑어보며 담아도 한 곳에 모입니다.

- 담긴 재료가 하나도 없으면 장보기 버튼(FAB) 자체가 안 보입니다
- 항목별로 뺄 수 있고, "모두 비우기"로 전체 삭제 가능
- "목록 복사하기"로 재료 목록을 텍스트로 복사해서 메모장 등에 붙여넣을 수 있음
  (브라우저가 클립보드 권한을 막는 환경에서는 "복사 실패" 안내가 뜨고, 이 경우
  텍스트를 직접 선택해서 복사하면 됩니다)
- 브라우저에 저장되어 새로고침해도 유지됩니다

### 쿠팡 연결
각 항목 옆에 **"쿠팡에서 사기"** 링크를 추가했습니다. 클릭하면 새 탭에서
`coupang.com/np/search?q=재료명` 형태의 쿠팡 검색 결과 페이지로 연결됩니다.

이건 **제휴 링크가 아니라 순수 검색 링크**입니다. 패널에도 "쿠팡 링크는
재료명으로 검색한 결과로 연결돼요 (제휴 링크 아님)"이라고 명시해뒀습니다.
나중에 수수료 수익까지 원하면 **쿠팡파트너스**에 별도로 가입해서 발급받는
추적 코드를 URL에 붙여야 하는데, 이건 계정 가입·인증이 필요한 부분이라
직접 진행해주셔야 합니다.

**코드 위치**: `index.html` 내 "장보기 리스트" 섹션 (`shoppingList`, `renderShop()`,
`coupangSearchUrl()` 등), 화면 우측 하단 고정 버튼(`#shopFab`)과 패널(`#shopPanel`)

---

## 7. 2인분 분량 변환 (3차)

레시피 상세의 "재료" 항목 옆에 **1인분 / 2인분** 토글을 추가했습니다.
2인분을 누르면 "70g(1/5모)" → "140g(0.4모)"처럼 계량이 자동으로 2배 환산되고,
영양정보(열량·탄수화물·단백질·지방·나트륨)도 함께 2배로 바뀝니다.

계량이 "20g(5마리)"처럼 자유 텍스트라 완벽한 환산은 불가능합니다. 문자열
안의 숫자(정수·소수·분수 포함)만 찾아 곱하는 방식이라, "약간"처럼 숫자가
없는 계량은 손대지 않고 그대로 둡니다. 2인분을 선택하면 "계량은 자동
환산한 값이라 실제와 소폭 다를 수 있어요"라는 안내를 함께 보여줘서, 정확한
계량이 아니라 참고용이라는 걸 분명히 했습니다.

**코드 위치**: `index.html` 내 `scaleAmount()`, `detailHtml(m, serves)`, `.serve-toggle`

---

## 8. 리뷰/평점 → "내 평가"로 범위를 좁혀 구현 (3차)

기획서의 "리뷰/평점"은 원래 여러 사람의 평가를 모으는 기능이겠지만, **이 앱은
서버 없이 브라우저에서만 도는 정적 페이지**라서 다른 사용자의 평가를 모을
방법이 없습니다. 없는 걸 있는 것처럼 보여주지 않기 위해, 범위를 "이 브라우저
에서만 기록되는 개인 별점(1~5점)"으로 좁혀서 구현했고, 화면에도
"이 브라우저에만 저장돼요"라고 명시했습니다.

실제로 여러 사용자의 리뷰를 모으려면 별도 백엔드(또는 최소한 공유
저장소)가 필요합니다 — 아래 "앞으로 고려하면 좋을 것" 참고.

**코드 위치**: `index.html` 내 "내 평가" 섹션 (`myRatings`, `.rating`, `.star`)

---

## 9. 재료 키(key) 정리 — 원본 데이터 노이즈 제거

### 무엇이 문제였나
사용자가 직접 확인해준 사례: "양파다진것", "양파(1/2개)" 처럼 조리법 서술어나
수량이 재료 키에 그대로 남아있는 경우, 그리고 "들기름"이 "양념들기름"이라는
별도의 키로 저장돼 있어서, 사용자가 "양파"나 "들기름"을 갖고 있어도 레시피가
요구하는 키(예: "양파다진것", "양념들기름")와 문자열이 정확히 일치하지 않아
매칭이 안 되는 문제였습니다.

### 실제 규모 확인
전체 재료 키 **1,660개**를 다 살펴보니, 이런 문제가 예상보다 넓게 퍼져있었습니다.

| 노이즈 종류 | 개수 | 예시 |
|---|---|---|
| 괄호 속 수량/설명 | 127개 | `양파(½개)`, `후춧가루(⅓Ts)` |
| 조리법 서술어 접미사 | 41개 | `양파다진것`, `아몬드슬라이스` |
| "양념" 접두사 오분류 | 22개 | `양념들기름`, `양념참기름`, `양념소금` |
| 기타(대괄호·글머리표 등 파싱 잔재) | 60개 | `•[추가재료]쌈두부`, `무가당코코아가루①` |

1,660개 중 약 250개(15%)가 이런 식으로 정리가 안 된 상태였습니다.

### 어떻게 고쳤나
`recipes.js`는 자동 생성물이라 파일 자체는 고치지 않고, **불러온 직후
`index.html`에서 한 번 정리**하도록 `cleanKey()` 함수를 추가했습니다.

1. 대괄호·글머리표(•)·동그라미 숫자(①②) 등 파싱 잔재 제거
2. 괄호 속 수량/설명 제거 — `양파(½개)` → `양파`
3. 조리법 서술어 접미사 제거 — `양파다진것` → `양파`
4. `tools/rules.json`의 `ingredientAliases`(153개) 재조회 — `후춧가루` → `후추`
5. "양념/저염/다진" 등 접두사 제거(남는 글자가 2자 이상일 때만) — `양념들기름` → `들기름`
6. 다시 한번 별칭 재조회

이 정리는 매칭에만 쓰이는 재료 **키**에만 적용되고, 레시피 상세에 보이는
원래 재료 **이름**(예: "연두부", "칵테일새우")은 그대로 유지됩니다.

**주의해서 뺀 규칙**: "채"를 접미사 목록에 넣으면 "청경채"→"청경",
"적채"→"적"처럼 완전히 다른 단어로 잘려버리는 걸 시뮬레이션으로 확인하고
뺐습니다. 정리 규칙은 실제 데이터로 먼저 검증한 뒤에만 반영했습니다.

### 결과 확인 (1차)
- 전체 고유 키: **1,660개 → 1,444개** (216개 감소)
- `들기름`: 기존 19회 + `양념들기름` 4회 → **23회로 정확히 합산**
  (레시피 "버섯콩불고기" 등에서 원래 이름이 "양념 들기름"이었던 재료가
  키 `들기름`으로 정상 매칭되는 것까지 직접 확인)
- 자동완성에서 "양파"를 검색하면 더 이상 "양파다진것"/"양파(½개)"가 따로
  뜨지 않고, "양파 501개 레시피"로 합쳐져서 나옴

같은 규칙(별칭 30여 개 추가 포함)은 `tools/rules.json`에도 문서로 남겨뒀습니다.
다만 `build-recipes.ps1`은 PowerShell 실행 환경이 없어 직접 돌려보고 수정할
수 없어서, 스크립트 코드는 건드리지 않고 설명만 추가했습니다. 나중에 PowerShell
환경에서 `recipes.js`를 다시 만들게 되면, `index.html`의 `cleanKey()`와 같은
규칙(접미사 제거 단계)을 빌드 스크립트의 NAMING 단계에도 옮겨 반영하는 걸
권장합니다 — 그러면 정리된 키가 파일 자체에 baked-in 되어 지금의 런타임
정리 로직 없이도 항상 깨끗한 상태로 시작할 수 있습니다.

### 9-2. "5개 이하 레시피에서만 쓰이는 재료" 전수 조사 → 대량 별칭 보강

사용자가 "재료 중에서 해당 재료를 사용하는 레시피가 5개 이하인 것 리스트로
알려줘"라고 요청해서 전수 조사를 해봤더니, 당시 기준 **1,660종 중 1,413종
(85%)** 이 레시피 5개 이하에서만 쓰이고 있었습니다. CSV로 뽑아 전달하면서
살펴보니, 진짜 희귀한 재료(락교·머위대 등)보다 **원본 데이터 파싱이 덜 된
항목**이 훨씬 많았습니다 — `돼지고기갈은것`/`돼지고기목심`/`돼지고기(불고기용)`
처럼 사실상 "돼지고기"인데 키만 갈라진 경우가 대표적입니다.

이 목록을 근거로 흔한 재료의 접두/접미 변형을 자동으로 찾아 `tools/rules.json`의
`ingredientAliases`에 **540개를 일괄 추가**했습니다 (153개 → 693개). 문자열은
겹치지만 실제로 다른 식재료인 경우는 자동 매칭에서 제외했습니다:

> `고구마순`(뿌리인 고구마와 다른 부위), `고추냉이`(와사비, 고추 아님),
> `레몬그라스`(레몬과 다른 식물), `감자튀김`(완제품), `아이스크림`(조리용
> 크림 아님), `코코넛우유`/`아몬드우유`(유제품 우유·견과류 자체와 다른 제품),
> `땅콩호박`(버터넛스쿼시, 땅콩 아님) 등 13건

또한 `양념다진마늘`, `양파다진것`, `후춧가루(` 처럼 재료명 **뒤에** 수식어가
붙어서 기존 규칙(접두어만 제거)이 못 잡던 것도 9건 별도로 바로잡았습니다.

### 9-3. "재료ooo" / "그룹명>재료" 패턴 추가 정리

이어서 사용자가 "`재료돼지고기` 같은 것도 다 정리됐어?"라고 확인 질문을 줘서
다시 세어보니, `재료닭가슴살`처럼 **"재료"가 접두사로 붙은 키 62개 중 27개**가
여전히 안 잡히고 있었습니다 (뒤에 오는 이름 자체가 흔한 재료 목록에 없어서
9-2의 자동 매칭 규칙이 못 걸렀던 것들).

하나씩 등록하는 대신 원인을 고쳤습니다 — `KEY_PREFIX_STRIP`(index.html)과
`modifierStrip`(rules.json)에 **"재료"** 자체를 접두어로 추가해서, 뒤에 어떤
재료가 오든 상관없이 걸러지도록 했습니다. 같은 김에 `주재료>바나나`,
`머랭>달걀흰자`처럼 **">"로 구획명이 붙은 23개**도 ">" 뒤쪽만 남기는 처리를
`cleanKey()`에 추가했습니다.

결과: 재료ooo 62개 → 1개(내용 없는 진짜 빈 라벨만 남음), ">" 패턴 23개 → 0개.
전체 고유 키 1,660개 → **1,369개**로 더 줄었습니다.

### 9-4. rules.json ↔ index.html 동기화 격차 발견 및 수정 (중요)

9-2에서 `tools/rules.json`에 넣은 540개를 실제로 다 반영했는지 재검증하는
과정에서, **709개 중 377개가 실제로는 브라우저에 적용되지 않고 있었다는 걸
발견했습니다.**

원인: `tools/rules.json`은 나중에 PowerShell로 `recipes.js`를 다시 만들 때만
쓰이는 파일이고, 지금 화면에 바로 적용되는 건 `index.html` 안에 따로 있는
`KEY_ALIASES`/`cleanKey()`입니다. 9-2 작업 때 `rules.json` 쪽만 고치고
`index.html`의 `KEY_ALIASES`에는 옮기는 걸 빠뜨렸던 것입니다. 예를 들어
`된장육수된장`은 rules.json엔 `"된장"`으로 등록해뒀지만, 접두사도 접미사도
아니라 `KEY_PREFIX_STRIP`/`KEY_SUFFIX_STRIP`으로는 못 잘라내는 형태라
`index.html`에 똑같이 등록하지 않으면 그대로 살아남는 구조였습니다.

두 표를 대조해서 값이 서로 어긋나는 건 없는지부터 확인한 뒤(충돌 0건),
누락된 377개를 `RULES_ALIASES_SYNC`라는 객체로 만들어 `KEY_ALIASES`에
병합했습니다. `단호박제육볶음`(원본 키: `재료돼지고기`) 레시피로 실제
검증까지 마쳤습니다 — "돼지고기"를 재료로 넣고 검색하면 이 레시피가
정상적으로 매칭됩니다.

### 최종 결과 확인
- 전체 고유 키: **1,660개 → 992개**
- 5개 이하 레시피에서만 쓰이는 재료: **1,413종 → 740종**
- `재료돼지고기` → `돼지고기`, `된장육수 된장` → `된장`으로 정상 변환되는 것을
  `window.RECIPE_DB`를 직접 조회해서 확인
- "돼지고기, 단호박, 양파, 대파"로 검색하면 관련 레시피들이 `✓ 돼지고기`로
  정상 매칭되는 것을 실제 화면에서 확인

**남은 한계**: 남은 740종은 대부분 락교·머위대류 진짜 희귀 재료이거나,
`2가지색미니파프리카30g`처럼 계량이 이름에 섞여 들어간 원본 파싱 오류라
알고리즘 하나로 일괄 처리하기보다 하나씩 봐야 합니다. 전체 목록은
[`재료_5개이하_레시피목록.csv`](./재료_5개이하_레시피목록.csv)로 남겨뒀습니다
(엑셀에서 한글이 깨지지 않도록 UTF-8 BOM을 넣어 저장했습니다 — 처음 보낸
버전은 BOM이 없어 한글이 깨져 보였던 걸 이후에 고쳤습니다).

**코드 위치**: `index.html` 내 `cleanKey()`, `KEY_ALIASES`, `KEY_PREFIX_STRIP`,
`KEY_SUFFIX_STRIP`, `RULES_ALIASES_SYNC` (DB 유효성 검사 직후, 재료 입력
섹션보다 앞), `tools/rules.json` 내 `modifierStrip`/`ingredientAliases`와
`_2b_runtimeKeyCleanup` 설명

---

## AI 연동 관련 메모

기획서에 "AI 연동은 레시피 생성이 아니라 추천 로직에만 적용할 것"이라는 원칙이
있어서, 2차·3차 작업에서는 AI를 전혀 쓰지 않았습니다 (전부 규칙 기반 로직).
나중에 AI를 붙이게 되면 이 원칙 — 새 레시피를 만들어내지 않고, 이미 있는
1,151건 중에서 더 잘 추천하는 데만 쓰는 것 — 을 지켜주세요.

---

## 참고 — 테스트 방법

이 앱은 파일을 더블클릭해서 `file://`로 직접 열면 `recipes.js`가 브라우저
보안 정책 때문에 로드되지 않을 수 있습니다. 그럴 때는 프로젝트 폴더에서
아래처럼 간단한 로컬 서버를 띄운 뒤 `http://localhost:8099`로 접속해서
확인하면 됩니다.

```bash
python3 -m http.server 8099
```

위 9개 항목 모두 이 방식으로 직접 켜서 동작을 확인했습니다.

---

## 앞으로 고려하면 좋을 것

- **인기 메뉴 / 여러 사용자 리뷰**: 둘 다 "여러 사용자의 데이터를 모아야
  의미가 있는" 기능인데, 지금 이 앱엔 그걸 모을 서버가 없습니다. 나중에
  백엔드를 붙이게 되면, `recipes.js`의 각 레시피에 `popularity` 값만 채우면
  1번에서 만들어둔 인기순 정렬이 바로 동작합니다. 리뷰도 비슷한 구조로
  확장할 수 있습니다.
- **재료 키 정리, 남은 부분**: 9번에서 1,660개 중 668개(40%)를 정리했지만,
  빈도가 낮은(1~2회) 드문 조합이나 원본 자체의 오타, 계량이 이름에 섞여
  들어간 파싱 오류까지 전부 잡지는 못했습니다 (남은 740종은
  [`재료_5개이하_레시피목록.csv`](./재료_5개이하_레시피목록.csv) 참고).
  `index.html`의 `cleanKey()` 규칙(`KEY_ALIASES`/`KEY_PREFIX_STRIP`/
  `KEY_SUFFIX_STRIP`)을 `tools/rules.json`의 `ingredientAliases`/
  `modifierStrip`에도 반영해서 `build-recipes.ps1`로 `recipes.js`를 다시
  만들면, 지금의 런타임 정리 로직 없이도 처음부터 깨끗한 키로 시작할 수
  있습니다. (PowerShell 실행 환경이 없어 이번엔 스크립트 자체는 수정하지
  못했습니다 — `tools/rules.json`의 `_2b_runtimeKeyCleanup` 항목에 옮길
  규칙을 정리해뒀습니다.) 참고로 `rules.json`과 `index.html`은 서로 다른
  시점에 따로 고치면 이번처럼 내용이 어긋날 수 있으니, 둘 중 하나를 고칠
  땐 다른 쪽도 함께 확인하는 게 안전합니다.
- **쿠팡 제휴 링크화**: 지금은 순수 검색 링크입니다. 수익화하려면 쿠팡파트너스
  가입 후 발급받는 추적 코드를 `coupangSearchUrl()`에 반영하면 됩니다.
