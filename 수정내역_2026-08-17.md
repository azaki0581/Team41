# 냉장고 털기 — 수정 내역 (2026-08-17)

> [`수정내역_2026-08-16.md`](./수정내역_2026-08-16.md) 에 이어지는 문서입니다.
> 이 파일에는 **2026-08-17 하루 동안 바뀐 것만** 담았습니다.
>
> 이날은 새 기능을 붙이는 대신, 앱을 실제로 켜서 추천 결과를 하나하나 뜯어보고
> **"화면이 말하는 것과 코드가 하는 일이 다른 곳"** 을 찾아 고쳤습니다.
> 실제 코드는 전부 [`index.html`](./index.html) 안의 `<script>` 태그 안에 있습니다.

## 목차

| # | 항목 | 성격 |
|---|---|---|
| 1 | 추천이 "내 냉장고 재료"를 거의 안 쓰던 문제 | 🔴 로직 결함 |
| 2 | "0.7초 만에 추천 완료"가 실제 계산 시간이 아니던 문제 | 🔴 지표 오류 |
| 3 | 밥 / 쌀이 별개 재료로 갈라져 있던 문제 | 🟠 데이터 |
| 4 | 보안 강화 (CSP · 배포 헤더 · 이스케이프 · 저장값 검증) | 🟠 보안 |
| 5 | 문서 동기화 (README) | ⚪ 문서 |

마지막에 **[발견했지만 이번에 고치지 않은 것](#발견했지만-이번에-고치지-않은-것)** 을 따로 정리해뒀습니다.

**바뀐 파일**: `index.html` · `tools/rules.json` · `README.md` · `vercel.json`(신규)
**`recipes.js` 는 건드리지 않았습니다.**

---

## 1. 추천이 "내 냉장고 재료"를 거의 안 쓰던 문제

### 무엇이 문제였나

계란 · 김치 · 밥을 넣고 추천을 받으면, 상위에 뜨는 요리들이 **내가 넣은 재료를
하나도 쓰지 않았습니다.**

| 순위(수정 전) | 요리 | 내 재료 사용 |
|---|---|---|
| 1위 | 불고기 소스를 곁들인 두부구이 | **0개** |
| 2위 | 아욱 된장무침 | **0개** |
| 3위 | 갈릭칩동태구이 | **0개** |

상위 20개 중 내 재료를 하나라도 쓰는 요리는 **4개(20%)** 뿐이었습니다.
"냉장고 털기"라는 서비스 이름과 정반대의 결과입니다.

게다가 `README.md` 가 *"이걸 빼면 재료가 1~2개뿐인 생소한 요리(매운락교무침)가
1순위로 올라옵니다"* 라고 경고해 둔 바로 그 **매운락교무침이 실제로 10위 안에**
있었습니다. 재료를 바꿔 돌려도 마찬가지였습니다 — 돼지고기·양파·대파·당근·감자로
돌리면 5위였습니다.

### 왜 이런 일이 생겼나

원인은 **두 가지가 겹친 것**이었습니다.

**① `have` 에는 양념과 물이 전부 들어간다**

`matchRecipe()` 의 `owns()` 는 "이 재료를 갖고 있는가"를 판정할 때
사용자가 입력한 재료 · 켜둔 양념 17종 · 물/얼음(`alwaysHave`)을 **구분 없이**
전부 "있음"으로 셉니다. 이건 *"지금 이걸 만들 수 있나?"* 를 판정하는 데는
올바른 방식입니다 (2026-08-16 문서 참고 — 주재료든 양념이든 없으면 사러
가야 하는 건 똑같으니까요).

문제는 **같은 값을 "냉장고를 얼마나 비우는가"에도 그대로 썼다**는 것입니다.
간장·소금·참기름만 쓰는 요리도 `have` 가 8~9개씩 잡히니, 정작 계란·김치·밥을
쓰는 요리보다 "재료를 많이 쓰는 요리"로 계산돼 버렸습니다.

**② 점수가 3순위 타이브레이크로만 쓰이고 있었다**

2026-08-16 에 정렬을 우선순위 단계로 분리하면서, 0순위를 **"부족한 재료 수"
하나**로 두었습니다. 그 결과:

```
양념만으로 되는 요리 + 모르는 재료 1개   →  부족 1개  →  이김
내 재료를 3개 다 쓰지만 2개 더 사야 함   →  부족 2개  →  짐
```

부족한 재료 수에서 이미 승부가 나버려서, 그 아래에 있던 "재료를 많이 쓰는
요리" 기준은 **동점일 때만** 겨우 비교되었습니다. 락교(전체에서 5회 미만
등장)에 대한 -4 감점도 마찬가지로 묻혔습니다.

### 어떻게 고쳤나

**① `mine` 을 따로 센다** — `matchRecipe()` 에 `mine` 배열을 추가했습니다.
사용자가 **입력창에 직접 담은 재료만** 들어갑니다. 체크해 둔 양념 17종과
물은 빠집니다.

```js
if (owned) {
  have.push(ing);
  // 사용자가 입력창에 직접 담은 재료. 양념 체크·물은 여기 안 들어간다
  if (myIngredients.indexOf(ing.key) !== -1) mine.push(ing);
}
```

**② 점수 기준을 `have` → `mine` 으로 바꾸고, 1순위로 올린다**

```
1순위  지금 당장 만들 수 있는가 (사야 할 것 0개)
2순위  점수 = 내 재료 사용 수 × 3 − 사야 할 것 × 2 − 구하기 어려운 재료 × 4
3순위  인기 메뉴(popularity) → 사야 할 것 적은 순 → 매칭률 → 조리시간 → 작업량
```

"바로 만들 수 있는 요리가 먼저"라는 원칙(2026-08-16 의 1번 항목)은 그대로
1순위에 남겨뒀습니다. 그 아래에서 냉장고를 얼마나 비우는지로 겨루게 했습니다.

**③ 내 재료를 하나도 안 쓰는 요리는 결과에서 뺀다**

아무리 만들기 쉬워도 그건 냉장고 털기가 아닙니다.

```js
var usesMine = all.filter(function (m) { return m.mine.length > 0; });
var pool = usesMine.length ? usesMine : all;
widened = usesMine.length === 0;   // 넓혔다는 사실은 화면에서 말해준다
```

**④ 넓혔으면 넓혔다고 말한다**

담은 재료를 쓰는 요리가 **하나도** 없으면 빈 화면을 보여주느니 범위를 넓히되,
그 사실을 결과 화면에 적습니다. 이 프로젝트가 지켜온 *"가정을 숨기지 않는다"*
원칙(양념 체크 안내와 같은 자리)을 그대로 따랐습니다.

> 집에 있다고 체크한 양념 17가지를 포함해서 계산했어요.
> **담으신 재료를 쓰는 요리를 찾지 못해서, 양념만으로 만들 수 있는 요리까지
> 넓혀서 보여드려요.**

### 결과 확인

재료: 돼지고기 · 양파 · 대파 · 당근 · 감자

| | 수정 전 | 수정 후 |
|---|---|---|
| 1위 | 불고기 소스를 곁들인 두부구이 (내 재료 **0개**) | 새송이등갈비 (**4개**) |
| 2위 | 아욱 된장무침 (**0개**) | 콩나물부추볶음 (**4개**) |
| 3위 | 갈릭칩동태구이 (**0개**) | 된장숙성저수분수육 (**3개**) |

재료: 계란 · 김치 · 밥 → 1~3위 모두 내 재료를 사용
(바나나를 감싼 일본식 달걀말이 / 멸치 누룽지과자 / 흑임자 두부국).
2페이지(4~6위)까지 넘겨서도 전부 내 재료를 쓰는 것을 확인했습니다.

**매운락교무침은 더 이상 상단에 나오지 않습니다.**

**코드 위치**: `matchRecipe()`, `search()`, `renderResults()`

---

## 2. "0.7초 만에 추천 완료"가 실제 계산 시간이 아니던 문제

### 무엇이 문제였나

결과 화면 우측 상단에 뜨는 추천 소요 시간이 **무엇을 입력하든 항상 "0.7초"**
였습니다. 재료를 1개 넣든 10개 넣든 같은 값이 나왔습니다.

이 숫자는 `README.md` 의 KPI 표(`재료 입력 → 추천까지 소요 시간 / 목표 3초
이내 / 현재 1.6초 ✅`)의 근거이자, 발표 시연에서 그대로 보여줄 값이었습니다.

### 왜 이런 일이 생겼나

결과가 튀어나오기 전에 로딩 연출을 0.7초 보여주는데, **그 `setTimeout` 콜백
안에서** 경과 시간을 읽고 있었습니다.

```js
var started = performance.now();
search();
setTimeout(function () {
  renderResults(performance.now() - started);   // ← 연출 700ms 가 포함됨
}, 700);
```

즉 화면에 뜬 건 추천 계산 시간이 아니라 **연출 시간(700ms) + α** 였습니다.
실제 `search()` 는 수십 ms 만에 끝납니다.

### 어떻게 고쳤나

`search()` 앞뒤만 재고, 연출 시간은 `LOADING_MS` 상수로 분리했습니다.

```js
var LOADING_MS = 700;   // 화면 연출용. 추천 계산과 무관하다

var started = performance.now();
search();
var elapsedMs = performance.now() - started;   // 실제 계산 시간만

setTimeout(function () {
  renderResults(elapsedMs);
}, LOADING_MS);
```

표시 형식도 바꿨습니다. 실제 계산 시간은 1초에 한참 못 미쳐서
`(ms/1000).toFixed(1)` 를 그대로 쓰면 `0.0초` 가 됩니다. 잰 것 같지도, 빠른
것 같지도 않아서 **1초 미만은 ms 로** 보여줍니다 (`elapsedText()`).

### 결과 확인

| | 수정 전 | 수정 후 |
|---|---|---|
| 재료 3개 (계란·김치·밥) | 0.7초 | **23~28ms** |
| 재료 5개 (돼지고기 외) | 0.7초 | **16ms** |
| 재료 1개 | 0.7초 | **17~24ms** |

입력에 따라 값이 실제로 움직입니다. `README.md` 의 KPI 표도 함께 고쳤고,
**"이 숫자는 계산 시간만이고, 사용자가 체감하는 시간은 여기에 연출 0.7초가
더해진 값"** 이라는 단서를 붙였습니다.

**코드 위치**: `$search` 클릭 핸들러, `elapsedText()`, `renderResults()`

---

## 3. 밥 / 쌀이 별개 재료로 갈라져 있던 문제

### 무엇이 문제였나

원본 데이터가 같은 성격의 요리를 어떤 데는 `쌀`, 어떤 데는 `밥`으로 적어둔
탓에 두 키가 따로 놀았습니다.

- `밥` 을 쓰는 레시피 **57건**
- `쌀` 을 쓰는 레시피 **56건**

사용자가 "밥"을 넣으면 **쌀로 짓는 레시피 56건이 통째로 빠졌습니다.**
반대도 마찬가지였고, 별칭 표에는 `쌀밥·찬밥·공기밥·즉석밥 → 밥` 은 있는데
정작 `쌀` 자체가 없었습니다. `볶음밥쌀` 처럼 파싱이 덜 된 키도 남아 있었습니다.

### 어떻게 고쳤나

`recipes.js` 는 자동 생성물이라 직접 고치지 않았습니다. 2026-08-16 에
만들어 둔 방식 그대로, **`index.html` 의 별칭 표와 `tools/rules.json` 양쪽에
같은 내용을 넣었습니다.**

| 파일 | 위치 | 역할 |
|---|---|---|
| `index.html` | `KEY_ALIASES` | 레시피 재료 키 정리 (`cleanKey()`) |
| `index.html` | `EXTRA_ALIASES` | 사용자가 입력창에 친 말 (`normalize()`) |
| `tools/rules.json` | `ingredientAliases` | 위 `KEY_ALIASES` 와 짝 |
| `tools/rules.json` | `userInputAliases` | 위 `EXTRA_ALIASES` 와 짝 |

**합친 것** → `밥`
`쌀` · `멥쌀` · `백미` · `볶음밥쌀` · `강황쌀` · `잡곡밥` · `오곡밥` · `귀리밥` ·
`잡곡밥흑미검은콩` · `재료쌀` (+ 입력용으로 `햇반`)

**일부러 합치지 않은 것**

| 합치지 않음 | 이유 |
|---|---|
| 찹쌀 · 현미 · 흑미 · 보리쌀 · 좁쌀 | 맛과 쓰임이 다른 곡물. 흰쌀밥 대신 못 씁니다 |
| 쌀가루 · 찹쌀가루 · 박력쌀가루 | 가루는 완전히 다른 재료입니다 |
| 쌀국수 · 쌀소면 · 쌀떡 · 쌀뜨물 · 쌀겨 | 이름만 겹칠 뿐입니다 |
| 곤약쌀 · 곤약미 | 쌀이 아닙니다 |

### 결과 확인

`밥` 으로 매칭되는 레시피 **57건 → 131건**.
입력창에 "쌀"을 쳐도 밥으로 담깁니다.
`찹쌀`(23건) · `현미`(28건) · `쌀가루`(10건) · `쌀국수`(2건)는 그대로 유지됨을
확인했습니다.

> **판단이 필요했던 부분** — 생쌀과 지은 밥은 엄밀히 다릅니다. 다만 이
> 데이터에서는 같은 요리를 두 이름으로 적어놓은 경우가 대부분이라 합치는
> 쪽이 실사용에 맞다고 봤습니다. 나중에 구분이 필요해지면 위 표의 네 곳에서
> 이 항목만 빼면 되돌릴 수 있습니다.

---

## 4. 보안 강화

배포(GitHub + Vercel)를 앞두고 점검하면서 함께 손봤습니다.

### 4-1. CSP(Content-Security-Policy) 추가

`index.html` 의 `<head>` 맨 위에, **이 페이지가 무엇을 불러올 수 있는지**를
브라우저에 직접 못박아 두는 `<meta>` 태그를 넣었습니다.

| 지시자 | 값 | 의미 |
|---|---|---|
| `default-src` | `'none'` | 기본은 전부 차단하고 필요한 것만 아래에서 연다 |
| `script-src` / `style-src` | `'self' 'unsafe-inline'` | 이 파일 안의 `<script>`·`<style>`. **외부 도메인은 차단** |
| `img-src` | `'self' data: 식약처도메인` | 조리 사진 + 파비콘만 허용 |
| `connect-src` | `'none'` | **fetch/XHR 전면 차단** |
| `form-action` / `frame-src` / `object-src` / `base-uri` | `'none'` | 폼 전송·iframe·플러그인·`<base>` 주입 차단 |

가장 의미 있는 건 **`connect-src 'none'`** 입니다. 이 앱은 원래 네트워크
요청을 하지 않습니다(레시피는 `recipes.js` 에 다 들어 있습니다). 이걸 명시해
두면 **입력한 재료와 별점이 브라우저 밖으로 나가지 않는다는 것을, 코드를
믿는 게 아니라 브라우저가 보장**합니다.

> **`'unsafe-inline'` 이 남아 있는 이유** — 앱 전체가 `index.html` 한 파일 안의
> `<script>`·`<style>` 로 되어 있습니다. 이걸 없애려면 JS/CSS를 별도 파일로
> 빼야 하는데, *"화면·문구·디자인은 index.html만 고치면 된다"* 는 이 프로젝트의
> 구조를 깨는 일이라 하지 않았습니다.

**적용 후 확인**: 조리 사진(썸네일 + 단계별 사진 18장) · 쿠팡 링크 · 장보기
리스트 · 별점 · 상세보기 모두 정상 동작. 콘솔 에러 0건.

### 4-2. `vercel.json` 신규 생성 — 클릭재킹 방어

클릭재킹 방어(`frame-ancestors`)는 **`<meta>` 태그로는 적용되지 않습니다.**
실제 HTTP 응답 헤더여야 합니다. 그래서 프로젝트 루트에 `vercel.json` 을
만들었습니다.

```json
{
  "headers": [{
    "source": "/(.*)",
    "headers": [
      { "key": "X-Frame-Options", "value": "DENY" },
      { "key": "Content-Security-Policy", "value": "frame-ancestors 'none'" },
      { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
      { "key": "X-Content-Type-Options", "value": "nosniff" }
    ]
  }]
}
```

Vercel 에 배포하면 자동 적용되고, **로컬에서 `index.html` 을 더블클릭할 때는
아무 영향이 없습니다.**

### 4-3. `escapeHtml()` — 작은따옴표 누락

```js
// 수정 전 : /[&<>"]/       ← ' 가 빠져 있음
// 수정 후 : /[&<>"']/
```

지금은 HTML 속성값을 전부 큰따옴표로 감싸고 있어서 **당장 뚫리는 취약점은
아닙니다.** 다만 나중에 누가 속성 하나를 작은따옴표로 쓰는 순간 조용히
구멍이 되는 자리라 미리 막아뒀습니다.

### 4-4. `localStorage` 저장값 타입 검증

`localStorage` 는 사용자가(또는 같은 도메인의 다른 스크립트가) 마음대로 고칠
수 있는 값입니다. 기존 코드는 `JSON.parse` 만 `try` 로 감쌌는데, **파싱은
성공했지만 타입이 기대와 다르면**(배열 자리에 숫자 등) 바로 다음 줄에서
터져 앱 전체가 멈췄습니다.

```js
function readStored(key, kind) {   // kind: "array" | "object"
  var v;
  try { v = JSON.parse(localStorage.getItem(key) || "null"); } catch (e) { return null; }
  if (v === null) return null;
  if (kind === "array")  return Array.isArray(v) ? v : null;
  if (kind === "object") return (typeof v === "object" && !Array.isArray(v)) ? v : null;
  return null;
}
```

`loadPantry()` · `loadShopping()` · `loadRatings()` 세 곳에 적용했습니다.

### 4-5. 점검했지만 문제없던 것

- **쿠팡 링크** — 이미 `encodeURIComponent()` + `rel="noopener noreferrer"` 로
  올바르게 되어 있었습니다.
- **레시피 데이터가 들어가는 `innerHTML`** — 요리명 · 재료명 · 조리 문장 ·
  이미지 URL · 팁 전부 `escapeHtml()` 을 거치고 있었습니다.
- **개인정보** — 재료·별점·장보기 리스트는 전부 `localStorage` 에만 저장되고
  어디로도 전송되지 않습니다 (4-1 의 `connect-src 'none'` 으로 이중 보장).

---

## 5. 문서 동기화 (README.md)

코드를 고치면서 **`README.md` 가 사실과 달라진 부분**을 함께 맞췄습니다.

| 위치 | 무엇이 달랐나 |
|---|---|
| 3. 추천 순서 | 적혀 있던 공식(`가진 재료 수 × 2 − …`)이 **실제 코드와 달랐습니다.** 코드에서는 이 점수가 3순위 타이브레이크로만 쓰이고 있었습니다 |
| 8. 원본 데이터 처리 | 밥/쌀 통합 항목과 "합치지 않은 것" 표 추가 |
| 7. 배포 | **보안 섹션 신설** — CSP 설명, `vercel.json` 안내 |
| 2. 파일 구조 | `vercel.json` 추가 |
| 9. KPI | `1.6초` → `16~25ms`, 그리고 이 숫자가 계산 시간만이라는 단서 |

각 항목에는 **"고치기 전에는 이랬다"** 는 설명을 함께 남겨서, 발표 때 질문이
들어와도 그대로 답할 수 있게 했습니다.

---

## 발견했지만 이번에 고치지 않은 것

앱을 켜서 점검하다 찾은 것들입니다. **판단이 필요하거나 범위가 커서** 이번엔
손대지 않았습니다.

### 🟠 판단이 필요한 것

**① `MAX_NEED` 를 3으로 둘 것인가**

계란·김치·밥을 **전부 쓰는** `찬밥김치달걀찜` 은 부족한 재료가 4개라서
`MAX_NEED = 3` 에 걸려 결과에서 빠집니다. 값을 4~5로 올리면 이런 요리가
살아나지만, **"사야 할 게 3개 이하"는 카드가 사용자에게 한 약속**이라
임의로 바꾸지 않았습니다. 팀에서 정하면 반영하면 됩니다.
(`index.html` 의 `var MAX_NEED = 3;`)

**② "지금 바로 만들 수 있는 요리 N개" 헤드라인이 거의 안 뜬다**

`readyCount` 가 0이면 "재료 한두 개만 더하면…" 으로 넘어가는데, 실측상
0인 경우가 대부분입니다.

| 입력 재료 | 바로 만들 수 있는 요리 |
|---|---|
| 계란 + 김치 + 밥 | 0개 |
| 계란 + 양파 + 당근 + 대파 + 두부 + 우유 (6종) | 1개 |

**시연에서 초록색 `바로 만들 수 있어요` 배지를 보여주려면 재료를 꽤 많이
넣어야 합니다.** ①과 연결된 문제이니, 발표 전에 어떤 재료 조합으로 시연할지
미리 정해두는 게 안전합니다.

### 🟡 데이터 품질

**③ 같은 재료가 한 레시피에 두 번 (1,151건 중 91건, 8%)**

원본이 주재료와 고명에 같은 재료를 따로 적어둔 경우입니다.
예: 해물볶음밥의 미나리, 부대된장찌개의 햄, 토마토스프파스타의 토마토.

칩이 두 번 뜨고, **매칭률(`rate`) 분모가 부풀어 실제보다 낮게** 나옵니다.
`tools/build-recipes.ps1` 의 `$ings += $i` 앞에서 `key` 기준으로 걸러주면
됩니다. (PowerShell 실행 환경이 없어 이번에도 스크립트는 손대지 못했습니다)

**④ 계량이 재료명으로 남은 것 10종**

`12g` · `8g` · `10g` · `3배식초0.5g` · `2가지색미니파프리카70g` · `2배양조식초1.5g` 등.
`README.md` 8번에 *"366종 → 10종"* 으로 이미 인정된 부분인데, 이 10개가
**자동완성 목록에도 뜹니다** ("1"을 치면 `12g` 가 나옵니다).
개수가 적으니 `rules.json` 에 제외 목록으로 넣는 게 빠릅니다.
`나무막대기`(밥핫도그)처럼 식재료가 아닌 것도 섞여 있습니다.

### ⚪ 사소한 것

| 내용 | 위치 |
|---|---|
| 양념 칩의 `aria-pressed` 가 첫 렌더에서 갱신되지 않아, 켜진 채 시작하는 17개가 스크린리더에는 전부 "꺼짐"으로 읽힙니다 (클릭하면 그때부터 맞습니다) | `renderPantry()` |
| 자동완성의 "N개 레시피" 숫자가 ③의 중복 때문에 실제보다 큽니다 | `ING_FREQ` 계산 |
| 재료를 0개 담고 추천 버튼을 누르면 아무 반응이 없습니다. 버튼 비활성화나 안내가 있으면 좋겠습니다 | `$search` 핸들러 |
| "다른 레시피 보기"가 마지막 페이지에서 말없이 1페이지로 되돌아갑니다 | `more` 버튼 핸들러 |

---

## 참고 — 테스트 방법 (2026-08-17 보강)

2026-08-16 문서에는 `python3 -m http.server 8099` 로 안내되어 있는데,
**확인해보니 작업 PC에 파이썬이 설치되어 있지 않았습니다** (`python` 은
Windows 스토어 안내 스텁만 뜹니다). 같은 상황이면 PowerShell 만으로도
정적 서버를 띄울 수 있습니다.

```powershell
$root = "이 프로젝트 폴더 경로"
$listener = New-Object System.Net.HttpListener
$listener.Prefixes.Add("http://localhost:8099/")
$listener.Start()
# 이하 파일을 읽어 응답하는 루프
```

> ⚠️ PowerShell 스크립트에 **한글 경로**를 쓸 때는 반드시 **UTF-8 BOM** 으로
> 저장하세요. Windows PowerShell 5.1 은 BOM 없는 파일을 ANSI 로 읽어서
> 경로가 깨지고 404 가 납니다 (`README.md` 6번의 경고와 같은 이유입니다).
> 이번 점검에서도 실제로 이 문제로 한 번 막혔습니다.

`.claude/launch.json` 도 `python3` 를 가리키고 있어 지금 환경에서는 동작하지
않습니다. 쓰지 않는 파일이라 그대로 뒀습니다.

**이번 수정 5개 항목 모두 이 방식으로 브라우저에서 직접 켜서 확인했습니다** —
추천 결과 · 페이지 넘김 · 상세보기 · 조리 사진 · 장보기 리스트 · 별점 ·
새로고침 후 저장값 복원 · 폴백 경로(없는 재료 입력) · 다크/라이트 모드.
콘솔 에러 0건.
